<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">@color/yellow</item>
        <item name="cardBackground">@color/blue</item>
        <item name="textColor">@color/grey</item>
        <item name="tintColor">@color/green</item>
        <item name="buttonColor">@color/red</item>

        <item name="android:windowAnimationStyle">@style/windowAnimationTrasnition</item>

        <item name="android:windowBackground">@color/colorPrimaryNight</item>

    </style>

    <style name="darkTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">@color/black</item>
        <item name="cardBackground">@color/grey</item>
        <item name="textColor">@color/white</item>
        <item name="tintColor">@color/white</item>
        <item name="buttonColor">@color/blue</item>

        <item name="android:windowAnimationStyle">@style/windowAnimationTrasnition</item>

    </style>

    <style name="windowAnimationTrasnition">

        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@android:color/transparent</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
